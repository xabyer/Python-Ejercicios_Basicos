'''
Escribir el resultado de la siguiente expresión: a^3 x (b^2 - 2ac) / 2b
'''

# Declaramos las variables y solicitamos los datos:

a = float(input("Intruduce el primer número: "))
b = float(input("Intruduce el primer segundo: "))
c = float(input("Intruduce el primer segundo: "))

# Operamos:

resultado = (a ** 3 * (b ** 2 - 2 * a * c)) / (2 * b)

prfloat(resultado)
